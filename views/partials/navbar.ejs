<nav class="navbar navbar-expand-md sticky-top navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">
      <img src="/logo.png" width="40px" height="40px">
      Next&nbsp;Engine
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="collapsibleNavbar">
      <% if (locals.payload && locals.payload.role === 'admin') { %>
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link" href="/new-route">Add&nbsp;route</a>
          </li>
        </ul>
      <% } %>
      <% if (locals.payload) { %>
        <ul class="navbar-nav">
          <li class="nav-item">
            <a class="nav-link" href="/reservations">My&nbsp;tickets</a>
          </li>
        </ul>
      <% } %>
      <ul class="navbar-nav w-100 justify-content-end">
        <% if (!locals.payload) { %>
          <li class="nav-item">
            <a class="nav-link" href="/registration">Sign&nbsp;up</a>
          </li>
          <li class="nav-item" id="login-tab">
              <a class="nav-link" id="login-tab-btn" href="/login">
                <i class="fa-solid fa-user" id="login-icon"></i>
                Login
              </a>
          </li>
        <% } else { %>
          <li class="nav-item">
            <p class="nav-link" id="username-field">
              <i class="fa-solid fa-user"></i>
              <%= payload.username %>
            </p>
          </li>
          <li class="nav-item" id="logout-tab">
            <button class="nav-link" id="logout-tab-btn" onclick="logout()">
              <i class="fa-solid fa-arrow-right-from-bracket"></i>
              Logout
            </button>
        </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>
